# Supabase の Project Settings > API から取得して .env.local にコピーする
# VITE_ が付いた変数はブラウザに埋め込まれる。anon key は公開前提のキーなので問題ないが、
# service_role キーはここにも .env.local にも絶対に書かない（RLS を無視できてしまう）。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
